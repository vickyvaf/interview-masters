{
  "name": "apps-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && (mkdir -p dist && cp public/_redirects dist/_redirects 2>/dev/null || true)",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@gradio/client": "^2.4.0",
    "@hono/node-server": "^1.13.7",
    "dotenv": "^16.4.5",
    "edge-tts": "^1.0.1",
    "hono": "^4.6.15"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
